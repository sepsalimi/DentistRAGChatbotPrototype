// Retains the concise settings route for the shared source configuration view.
import { SourceSettings } from "@/components/SourceSettings";

export default function SettingsPage() {
  return <SourceSettings />;
}
